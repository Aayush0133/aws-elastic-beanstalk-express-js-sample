pipeline {
    agent any
    stages {
        stage('Test Docker') {
            steps {
                sh 'docker --version'  // This should display the Docker version in the Jenkins logs
            }
        }
    }
}
